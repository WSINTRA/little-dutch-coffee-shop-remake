import React from "react";


const LandingBox = (props) => (
 <div className="landing"> 
  <h1>Online dispensary for all your cannabis needs</h1>
  <h2>You must be 21 or older to enter and live in a state in which recreational usage is legal to order</h2>
  <div className="landing__login">LOGIN</div>
  <div className="landing__register">REGISTER</div>
</div>
);

export default LandingBox;


